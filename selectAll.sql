-- קבלת כל הנתונים מטבלת Event
SELECT * FROM Event;

-- קבלת כל הנתונים מטבלת RolesP
SELECT * FROM RolesP;

-- קבלת כל הנתונים מטבלת EmergencyDrill
SELECT * FROM EmergencyDrill;

-- קבלת כל הנתונים מטבלת EmergenBodies
SELECT * FROM EmergenBodies;

-- קבלת כל הנתונים מטבלת EvDebriefing
SELECT * FROM EvDebriefing;

-- קבלת כל הנתונים מטבלת TakingPart
SELECT * FROM TakingPart;

-- קבלת כל הנתונים מטבלת TeamP
SELECT * FROM TeamP;

-- קבלת כל הנתונים מטבלת Worker
SELECT * FROM Worker;

-- קבלת כל הנתונים מטבלת ActIn
SELECT * FROM ActIn;

-- קבלת כל הנתונים מטבלת Reoprt
SELECT * FROM Reoprt;

-- קבלת כל הנתונים מטבלת Practicing
SELECT * FROM Practicing;
